import { AuthorizationHttpInterceptor } from './authorization-http-interceptor';

describe('AuthorizationHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthorizationHttpInterceptor()).toBeTruthy();
  });
});
